{
  "name": "human-robot-communication-experiment",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev:web": "npm --prefix web run dev",
    "dev:server": "npm --prefix server run dev",
    "install:all": "npm install --prefix web && npm install --prefix server",
    "build": "npm run install:all && npm --prefix web run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "supabase": "^2.98.2"
  }
}
